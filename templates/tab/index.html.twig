{% extends 'base.html.twig' %}

{% block title %}Hello TabController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .table{border: 2px black solid;align-items: center}
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/xampp/htdocs/app/src/Controller/TabController.php'|file_link(0) }}">src/Controller/TabController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/xampp/htdocs/app/templates/tab/index.html.twig'|file_link(0) }}">templates/tab/index.html.twig</a></code></li>
    </ul>
    <table class="table">
         {% for element in  tab %}
             <tr>
        <td class="table">
            {{ element }}
        </td>
             </tr>
        {% endfor %}
    </table>
    <h3>{{ (tab|length) | default_carre }}</h3>
    <h3>{{ max(tab) }}</h3>
    <h3>{{ min(tab) }}</h3>
</div>
{% endblock %}
